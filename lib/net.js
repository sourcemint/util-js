
/*
    function parseHeaders(str) {
    	var headers = {};
    	var m;
    	str.split("\n").forEach(function(line) {
    		if ((m = line.match(/^([^:]*):\s*(.*?)\r?$/))) {
	    		headers[m[1]] = m[2];
	    	}
    	});
    	return headers;
    }
*/
